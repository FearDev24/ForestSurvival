# arte original (fonte)

PNGs como saíram do gerador, **antes** da limpeza de resíduo de chroma key.

O `.gdignore` impede a Godot de importar estas cópias: o jogo carrega apenas os
arquivos limpos. Mesmo padrão de `assets/characters/inimigos/_raw/` e
`assets/characters/frames/`.

Procedimento em `docs/ASSET_WORKFLOW.md`, seção "Limpeza de resíduo de chroma key".
